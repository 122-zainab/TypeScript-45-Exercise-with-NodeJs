//  stored a person name with whitespace characters at the begining and end of the name.
let whitespaceName = "\n\t Zainab Rasheed \t\n";

console.log(whitespaceName);


// stored a person name without whitespaces
let withoutwhitespaceName = whitespaceName.trim();

console.log(withoutwhitespaceName);